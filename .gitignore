# Environment — secrets must never be committed
.env
.env.local
.env.*.local

# Vercel
.vercel

# Node
node_modules/
npm-debug.log*

# macOS
.DS_Store

# Pitch output artifacts (generated, not source)
pitches/*/
